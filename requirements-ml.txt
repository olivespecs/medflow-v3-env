# Full ML stack for local development, Colab, or high-resource deployments.
# Enables SentenceTransformers semantic scoring and BERTScore in Task 4.
#
# Usage:
#   pip install -r requirements.txt          # core (Docker/HF Spaces)
#   pip install -r requirements-ml.txt       # add ML stack on top
#
# Or via pyproject.toml extras:
#   pip install -e ".[ml]"
#
# Environment variables to activate (already enabled by default when packages are present):
#   ENABLE_SENTENCE_TRANSFORMERS=1   (uses all-MiniLM-L6-v2, ~80MB)
#   ENABLE_BERT_SCORE=1              (uses distilbert-base-uncased)

# Core deps must already be installed from requirements.txt
-r requirements.txt

# ML stack
torch>=2.2.0,<3.0.0
transformers>=4.38.0,<5.0.0
evaluate>=0.4.0,<1.0.0
bert-score>=0.3.13,<1.0.0
sentence-transformers>=2.7.0,<4.0.0

# Dev / testing
pytest>=8.0.0,<10.0.0
hypothesis>=6.100.0
